#!/usr/bin/env php
<?php

include dirname(__DIR__) . DIRECTORY_SEPARATOR . 'bootstrap.php';

use Funbox\Framework\Console\Application;

$app = Application::create();

$status = $app->run($argv, $argc);

exit($status);